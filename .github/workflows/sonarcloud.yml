name: SonarCloud

on:
  push:
    branches: [master]
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarcloud:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          # Ephemeral CI-only database: allow passwordless local connections so
          # no credential literal is needed (keeps secret scanners happy).
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # `cache: pip` would write an Actions cache entry per ref — PR scopes that
      # nothing else can read and that only crowd the repo's 10 GB budget.
      # warm-build-cache.yml is the single writer; installing from PyPI costs a
      # few seconds a run and leaves nothing behind.
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-asyncio pytest-cov

      - name: Run tests with coverage
        env:
          DATABASE_URL: postgresql://postgres@localhost:5432/postgres
        run: |
          mkdir -p logs
          pytest --cov=. --cov-report=xml --cov-branch

      - name: SonarCloud Scan
        uses: sonarsource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
